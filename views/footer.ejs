	<script>
		$(function(){
			if($('.ul.success.message').length > 0){
				$('.ui.success.message').fadeOut(3000);
			}else if($('.ul.error.message').length > 0){
				$('.ul.error.message').fadeOut(3000);
			}
		    //  点击按钮弹出下拉框
			$('.ui.dropdown').dropdown();

			// 鼠标悬浮在头像上，弹出气泡提示框
			$('.post-content .avatar-link').popup({
			inline: true,
			position: 'bottom right',
			lastResort: 'bottom right'
			});
		})
	</script>
	</body>
</html>